name: demo-new

on:
  workflow_call:
    inputs:
      input1:
        required: false
        type: boolean

jobs:
  demo:
    runs-on: ubuntu-latest
    steps:
      - name: Check input
        run: echo 123
