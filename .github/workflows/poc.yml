name: demo-new

on:
  workflow_dispatch:
    inputs:
      input1:
        required: false
        type: string

jobs:
  demo:
    runs-on: ubuntu-latest
    steps:
      - name: Check input
        if: inputs.input1
        env:
          check: ${{ inputs.input1 }}
        run: echo ${{ env.check }}
