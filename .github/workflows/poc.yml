name: demo-new

on:
  workflow_dispatch:
  workflow_call:
    inputs:
      input1:
        required: false
        type: boolean

jobs:
  demo:
    runs-on: ubuntu-latest
    env:
      INPUT: ${{ inputs.input1 }}
    steps:
      - name: Upload playwright report
        uses: ./.github/actions/poc_if
        with:
          input2: 456