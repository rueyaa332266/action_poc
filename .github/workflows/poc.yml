name: demo-new

on:
  workflow_dispatch:
    inputs:
      input1:
        default: "test"

jobs:
  demo:
    runs-on: ubuntu-latest
    env:
      NBF: ${{ false }}
    steps:
      - name: Check input
        if: env.NBF
        run: echo ${{ env.NBF }}
