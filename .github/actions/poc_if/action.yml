name: Check if run

inputs:
  input2:
    required: true
    description: check

runs:
  using: 'composite'
  steps:
    - name: echo
      run: echo ${{ inputs.input2 }}
      shell: bash
    - name: Check 123
      if: inputs.input2 == '123'
      run: echo ${{ inputs.input2 }}
      shell: bash
    - name: Check 456
      if: inputs.input2 == '456'
      run: echo ${{ inputs.input2 }}
      shell: bash