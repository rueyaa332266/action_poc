name: demo-com

runs:
    using: 'composite'
    steps:
    - name: Checkout
      uses: actions/checkout@v3
    - name: check repo
      run: echo ${{ github.repository }}
    - name: check secret
      run: echo ${{ secrets.CHECK }}
    - name: check vars
      run: echo ${{ vars.CHECK }}
    - name: check files
      run: ls .github/workflows
